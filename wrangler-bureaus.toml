# =============================================================
# Config do Cloudflare Worker — DRG-Rently Bureaus de Crédito
#
# Proxy stateless para Quod Score Imobiliária e Idwall Background Check.
# Cada tenant traz as próprias credenciais via headers.
#
# Setup no Cloudflare (one-time):
#   - Sem secrets nem variables: o worker é puramente proxy
#   - As credenciais ficam em tenants/{id}/config/site e o app
#     envia em cada request
# =============================================================
name = "drg-rently-bureaus"
main = "cloudflare-worker-bureaus.js"
compatibility_date = "2024-09-01"
